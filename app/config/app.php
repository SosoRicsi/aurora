<?php

/**
 * The applications settings.
 * Use the config() helper or the Config::get() facade to reach a value.
 */
return [

	/* The applications name */
	'name' => env('APP_NAME', 'Aurora'),

	/* The applciations version */
	'version' => env('APP_VERSION', '0.0.0'),

	/* The applciations locale */
	'locale' => env('APP_LOCALE', 'en'),

	/* The applications envireonment */
	'env' => env('APP_ENV', 'local'),

];
